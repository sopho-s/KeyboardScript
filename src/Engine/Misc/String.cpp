#include "String.h"

namespace Engine {
    namespace Misc {
        int Contains(std::string string, std::string key) {
            for (int i = 0; i < string.length() - key.length(); )
        }
    }
}